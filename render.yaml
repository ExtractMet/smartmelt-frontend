# Optional: host this frontend on Render as a Static Site instead of GitHub Pages.
services:
  - type: web
    name: smartmelt-frontend
    runtime: static
    branch: main
    buildCommand: ""
    staticPublishPath: .
