/* ============================================================================
 * ☆ NEW — SmartMelt frontend deployment configuration
 * ----------------------------------------------------------------------------
 * Point this at your deployed BACKEND (the Render service URL). This is the one
 * line you must edit after deploying the backend.
 *
 *   window.SMARTMELT_API_BASE = "https://smartmelt-api.onrender.com";
 *
 * • For the GitHub Pages + Render split (this package): it MUST be your Render URL.
 * • Leave "" ONLY if the backend serves this page from the same origin
 *   (the earlier combined local mode).
 * • No trailing slash needed (it is trimmed automatically).
 * ==========================================================================*/
window.SMARTMELT_API_BASE = "https://REPLACE-WITH-YOUR-BACKEND.onrender.com";
