/* ☆ SmartMelt frontend deployment configuration
   Point this at your deployed BACKEND (the Render service URL) — the one line you edit.
       window.SMARTMELT_API_BASE = "https://smartmelt-api.onrender.com";
   No trailing slash needed (it is trimmed automatically). */
window.SMARTMELT_API_BASE = "https://smartmelt-backend.onrender.com";
